<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.cdut.dao.SetmealDao">
    <!--插入套餐数据 -->
    <insert id="add" parameterType="com.cdut.pojo.Setmeal">
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="id">
            SELECT LAST_INSERT_ID()
        </selectKey>
        insert into t_setmeal
        (code,name,sex,age,helpCode,price,remark,attention,img)
        values
        (#{code},#{name},#{sex},#{age},#{helpCode},#{price},#{remark},#{attention},#{img})
    </insert>
    <!--绑定套餐和检查组多对多关系-->
    <insert id="setSetmealAndCheckGroup" parameterType="hashmap">
        insert into t_setmeal_checkgroup
            (setmeal_id,checkgroup_id)
        values
            (#{setmeal_id},#{checkgroup_id})
    </insert>
    <update id="edit">
        update t_setmeal
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="code != null">
                code = #{code},
            </if>
            <if test="sex != null">
                sex = #{sex},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="helpCode != null">
                helpCode = #{helpCode},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
            <if test="attention != null">
                attention = #{attention},
            </if>
            <if test="img != null">
                img = #{img},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteAssociation">
        delete from t_setmeal_checkgroup where setmeal_id = #{id}
    </delete>
    <delete id="deleteById">
        delete from t_setmeal where id = #{id}
    </delete>
    <!--根据条件进行查询-->
    <select id="findByCondition" parameterType="string" resultType="com.cdut.pojo.Setmeal">
        select * from t_setmeal
        <if test="value != null and value.length > 0">
            where code = #{value} or name = #{value} or helpCode = #{value}
        </if>
    </select>

    <select id="findSetmealCount" resultType="map">
        select s.name,count(o.id) as value
        from t_order o ,t_setmeal s
        where o.setmeal_id = s.id
        group by s.name
    </select>
    <select id="findById" resultType="com.cdut.pojo.Setmeal">
        select * from t_setmeal where id = #{id}
    </select>
    <select id="findCheckGroupIdsBySetmealId" resultType="java.lang.Integer">
        select checkgroup_id from t_setmeal_checkgroup where setmeal_id = #{id}
    </select>
    <select id="findAll" resultType="com.cdut.pojo.Setmeal">
        select * from t_setmeal
    </select>
</mapper>